# PROGRESS

Running log for the FORGE build. One line per completed chunk: `[phase] what was built · files touched · next step`.
Read this first in every new session.

---

## PHASE 0 — Environment & Skeleton ✅ COMPLETE (17 Aug 2026)

- `[0.1]` Prerequisite audit + installs · nothing touched in repo · **Installed via winget:** Node.js 24.19.0 LTS, .NET 8 SDK 8.0.424 (only the runtime was present), Git 2.55.0, SQL Server 2022 Express (new `SQLEXPRESS` instance), SSMS 20.2.1. Also installed `dotnet-ef` 8.0.11 global tool. An unrelated SQL Server **2008 R2** instance (`MSSQL$RELYONDB`, third-party payroll software) was found and deliberately left alone — too old for EF Core 8 and not ours.
- `[0.2]` Client scaffold · `client/**` · Vite 8 + React 19 + TS 6, Tailwind v4 via `@tailwindcss/vite`, motion, lucide-react, TanStack Query, axios, React Router 7, RHF, Zod, Recharts. Vite proxies `/api`, `/media/uploads`, `/hubs` to the API so the client is same-origin with it.
- `[0.3]` Design tokens · `client/src/styles/tokens.css`, `index.css` · Full 03 §2–3 palette, type scale, geometry, motion and layout tokens as CSS custom properties, bridged into Tailwind's `@theme` so every utility resolves through them. Utilities: `.graded` (the one-grade rule), `.grain` (inline SVG `feTurbulence` at 4.5%), `.marquee-track`, `.text-outline`, `.skeleton`, `.underline-slide`, `.auth-input`, `.theme-light` (admin surface), gold selection + custom scrollbar, global `prefers-reduced-motion` short-circuit.
- `[0.4]` UI primitives · `client/src/components/ui/**` · `<Icon>` (inline SVG only — Lucide plus 14 hand-authored brand marks: barbell, kettlebell, boxing glove, body-scan, shaker, stretch, lotus, core, run, studio, spa, and the three social glyphs Lucide v1 dropped), `<Reveal>`/`<RevealGroup>`/`<KineticHeading>`, `<Button>`/`<ButtonLink>` (triple-intent variants + magnetic hover), `<Card>`/`<CardMedia>`/`<Badge>`/`<CapacityRing>`, `<Skeleton>`/`<EmptyState>`. Fonts: Clash Display (Fontshare, real variable face) + Inter variable, both preconnected with `display=swap`.
- `[0.5]` Domain model · `server/Gym.Core/**` · 47 entities across the full 04 §3 schema (identity, gym ops, billing+GST, scheduling, attendance, CRM, training, commerce, engagement, CMS) plus 30 enums, `RoleNames`, and the `IClock`/`IMediaStorage` seams.
- `[0.6]` Persistence · `server/Gym.Infrastructure/Persistence/**` · `GymDbContext` (schemas `gym` + `auth`), 47 fluent configurations with unique constraints and covering indexes on the hot paths (bookings by session, check-ins by branch+date, subscriptions by status+end, invoices by status+due, PR lookup by member+exercise+e1RM), global decimal/string conventions, audit stamping. Migration `Init` applied to `localhost\SQLEXPRESS` / `GymDb`.
- `[0.7]` Seeder · `server/Gym.Infrastructure/Persistence/Seeding/**` · Deterministic and idempotent (fixed RNG seed; every step short-circuits if its table has rows). Produces **3 branches, 8 trainers, 10 class formats, 15 rooms, 6 plans with Indian pricing + per-branch overrides, 40 classes/week (verified 0 trainer and 0 room conflicts), 458 materialised sessions, 200 members with realistic Indian names, 200 subscriptions/invoices/payments with 5% GST split, 6,673 bookings, 6,320 check-ins, 35 exercises, 14 products with mixed GST slabs, 10 badges, 3 coupons, 51 leads with follow-up sequences, 14 CMS pages / 63 sections, 8 testimonials, 5 transformations, 5 journal posts, 14 FAQs, 44 site settings**. Attendance drives streaks, which drive churn scores — the numbers agree with each other rather than being independently random.
- `[0.8]` Auth end-to-end · `Gym.Infrastructure/Services/TokenService.cs`, `Gym.Api/Controllers/AuthController.cs`, `Gym.Api/Program.cs`, `client/src/lib/{api,auth}.tsx`, `client/src/app/guards.tsx` · Identity + JWT (15 min) + rotating refresh (7 d, httpOnly cookie, SHA-256 hashed at rest). Reuse of an already-rotated token revokes the whole token family. Register / login (by email **or** mobile) / refresh / logout / me / change-password. Client keeps the access token in memory only, with a single-flight silent refresh on 401. Route guards both sides; `[Authorize(Roles=…)]` server-side.
- `[0.9]` CMS read+write API · `Gym.Api/Controllers/CmsController.cs`, `BranchesController.cs` · Anonymous published reads, Admin-only writes to a draft slot with explicit publish, drag-reorder, per-section visibility, site settings, branch directory and derived occupancy. `OccupancyHub` SignalR contract established so Phases 1–4 build against a stable client-method surface.
- `[0.10]` Public shell, CMS-driven · `client/src/features/public/**`, `client/src/app/router.tsx` · Header (nav by experience, scroll-reactive, mobile sheet), announcement bar, footer with per-branch detail read from the API, and `CmsRoutePage` — one component that renders whatever sections the CMS returns, in CMS order, through a section registry. **5 of 24 section types are registered** (Hero with video+poster+kinetic headline, MarqueeTicker, StatBand with count-ups, Manifesto, CtaBanner); the other 19 are seeded and wired but render nothing until Phase 1 adds them. Per-page SEO, OG tags and per-branch JSON-LD all come from `CmsPages`.
- `[0.11]` Auth + portal + admin shells · `client/src/features/{auth,member,admin,shared}/**` · Split-panel login/register on the same tokens, member portal home (dark, live occupancy from the API), admin home (light surface, same tokens), 403/404 pages.
- `[0.12]` Media pipeline · `client/media.manifest.json`, `client/scripts/fetch-media.mjs` · `npm run media` downloads 16 verified 4K/2K Unsplash assets into `client/public/media` so no binaries are committed and no page falls back to a grey box. Owner-uploaded media is separate: `LocalMediaStorage` writes WebP renditions + a blur placeholder to the API's `wwwroot/media/uploads`, served at `/media/uploads`.

### Phase 0 verification — all gates pass

| Gate | Result |
|---|---|
| `dotnet build Gym.sln` | 0 errors, 0 warnings |
| `npm run build` | passes; vendor chunks split (react / motion / data) |
| API runs, Swagger lists auth + cms | 18 endpoints: 6 × `/api/auth`, 8 × `/api/cms`, 3 × `/api/branches`, `/health` |
| Migration applied to `localhost\SQLEXPRESS` | `GymDb`, 55 tables (47 `gym` + 8 `auth`) |
| Seeded admin login | 200, role `Admin`, `mustChangePassword: true` |
| Self-registered member | 200, role `Member`, code continues the branch series (`FRG-WHF-00056`) |
| Seeded member login (email and mobile) | 200 both ways |
| Refresh rotation | new cookie issued; replayed old token → 401; post-logout refresh → 401 |
| Role enforcement | member → CMS write 403; admin → 204 |
| Client shell | renders at `:5173`, CMS content and media both resolve through the proxy |

### Credentials (dev)

- **Admin** — `admin@gym.local` / `Forge@Admin2026!` (set in `appsettings.Development.json`; forced change on first login)
- **Members** — any seeded member email (e.g. `rakesh.chopra@example.com`) / `Member@12345`

### Deviations from the docs — each with its reason

1. **Kinetic headline animates weight 400→700, not 400→800** (03 §3). Clash Display's real variable axis is 200–700; 800 does not exist in the font.
2. **Solution is `Gym.sln`, not `Gym.Api.sln`.** 04 §2's tree and §5's `dotnet new sln -n Gym` command disagree; followed the command.
3. **Build-time brand imagery lives in `client/public/media`; owner uploads live at `/media/uploads`.** The standing rule puts stock imagery in `client/public/media` while 04 §1 puts uploads in `wwwroot/media` — both would occupy `/media/*`. Splitting the upload namespace keeps them from shadowing each other.
4. **Public route slugs are `/plans`, `/journal`, `/tools`** (not `/pricing`, `/blog`, `/calculators`). Nav-by-experience per 03 §1; the CMS page slugs match, so Phase 1 should use these.
5. **ImageSharp pinned to 3.1.12, not 4.x.** v4 refuses to build without a paid Six Labors licence key; 3.1.12 is the latest patched release under the Split Licence. **Flag for the client:** even 3.1.x requires a commercial licence above a revenue threshold — worth confirming before launch, or swapping `IMediaStorage` for a different encoder.
6. **Invoices and Payments do not cascade-delete with a Member** (`NoAction`, not `Cascade`). The NFRs demand a non-repudiable payment audit trail, so financial rows must outlive the member record. Members are retired by `Status`, not deleted.
7. **Refresh cookie drops its `Secure` flag in Development only** (`!IsDevelopment() || Request.IsHttps`). Local dev runs over `http://localhost` through the Vite proxy, where a `Secure` cookie is silently discarded and silent refresh breaks. Always `Secure` outside Development.
8. **Day-pass and trial are sold as POS products, not as two of the 6 plans.** The spec asks for exactly 6 plans *and* lists DayPass/Trial plan kinds; the day pass and 5-visit guest pass are seeded in `Products` with SAC 999723 so the 6 headline plans stay the ones on the pricing page. Both `PlanKind` values exist for later use.
9. **`Branch.MemberCodePrefix` added to the schema.** Deriving the prefix from the slug gave Whitefield two series (`WHF` in the seeder, `WHI` at registration) with independent counters. Storing it makes one series per branch, including branches the owner adds later.
10. **Hero video not yet sourced.** `forge-hero.mp4/.webm` are referenced by the CMS but pending — the hero renders its poster, which is the designed fallback, so nothing shows as empty. Phase 1's imagery pass fills it.

### What Phase 1 inherits (nothing here needs redoing)

- Every public page's content already exists in `CmsSections` with real, specific copy — including the 19 section types with no renderer yet. Phase 1 registers components against seeded content rather than inventing it.
- `media.manifest.json` lists exactly which asset groups are still unsourced.
- `CapacityRing`, `Badge`, `CardMedia` (hover image swap), `Reveal`, `KineticHeading` and the occupancy endpoint + SignalR contract are all in place for the class rail, timetable and occupancy meter.

**Next step — PROMPT 1:** build the complete public website (all 12 features of Module 1) by registering the remaining 19 section renderers against the already-seeded CMS content, and complete the imagery pass listed under `pending` in `media.manifest.json`.

---

## PHASE 1 — Public Website ✅ COMPLETE (17 Aug 2026)

- `[1.1]` Public read API · `Gym.Api/Contracts/PublicContracts.cs`, `Controllers/{Classes,Trainers,Plans,Content}Controller.cs` · The data the marketing sections render from, all anonymous: class formats (each with its live session count, the branches that run it and the next bookable slot), the filterable timetable with facets, coach profiles, per-branch plan pricing, the seasonal offer, testimonials, transformations, FAQs and the journal. Times are returned twice — the UTC instant for ordering, and the IST wall clock for anything a visitor reads — so a Bengaluru timetable says "6:30 PM" whichever timezone it is browsed from.
- `[1.2]` Lead capture · `Controllers/LeadsController.cs` · `POST /api/leads` feeds the same pipeline the Phase 2 board reads, with the automated follow-up sequence already scheduled (5-minute WhatsApp, 24-hour call, evening-before reminder and post-trial call when a date is given). Phone normalisation to ten digits, a 24-hour dedupe so a double-tap is one lead, a honeypot that answers 201 without storing, and `+91` handling.
- `[1.3]` SEO endpoints · `Controllers/SeoController.cs` · `/sitemap.xml` generated from the CMS (27 URLs: 14 pages, 8 coaches, 5 posts) with per-route priorities, and `/robots.txt` disallowing the authenticated trees. A page the owner adds or unpublishes enters or leaves the index without a deploy.
- `[1.4]` Client data layer · `client/src/lib/public-api.ts` · One Zod-validated hook per resource. Marketing content caches for 10 minutes; the timetable for 60 seconds and keeps previous results on screen through a filter change, so the sheet never blinks back to a skeleton once it has content.
- `[1.5]` All 19 remaining section renderers · `client/src/features/public/sections/**` · AmenityBento (mixed 1×1/2×1/1×2/2×2 tiles, photo tiles scrimmed and text tiles inverted), ClassRail, TrainerHighlight, TransformationSlider, TestimonialWall, PricingTable, FaqAccordion, BranchLocator, AppQr, RichText, ImageFeature, BlogRail, CalculatorBlock, ContactBlock, OfferBanner, TimetableEmbed, AnnotatedFacility, LeadForm, SignatureScroll. **All 24 section types now render**; the registry is the only place a type binds to a component.
- `[1.6]` Shared public components · `client/src/features/public/components/**` · `SectionHeader` (one eyebrow/headline/lead stack so section rhythm is identical down the page), `OccupancyMeter` (240° SVG gauge, Comfortable/Busy/Peak, plus a compact chip), `ClassFormatCard` + `ClassSessionCard` (cult.fit's density at 2–3× the image size, capacity ring, spots-left urgency colouring), `TrainerCard` (3:4 portrait going duotone on hover via a CSS filter chain, not a second asset), `BeforeAfterSlider` (both frames clipped from one box so the comparison stays pixel-aligned; a real range input underneath carries the keyboard and screen-reader semantics).
- `[1.7]` Signature scroll moment · `SignatureScrollSection.tsx` · The one on the site (03 §6): the Whitefield floor scales 80%→100vw pinned while the headline's tracking tightens 0.14em→-0.02em. Collapses to a static full-bleed frame under `prefers-reduced-motion` — a valid final frame, not a degraded one.
- `[1.8]` Timetable · `TimetableEmbedSection.tsx` · Day tabs with per-day counts, filter pills for branch/format/coach/level/time-of-day, sessions grouped by day, skeletons not spinners. Facets come back with the results so a filter never offers a combination that returns nothing; branch pages pin their own branch and hide the control rather than pre-setting it.
- `[1.9]` Pricing · `PricingTableSection.tsx` · Three tiers on screen with the popular one gold-bordered, trust microcopy under every price, a branch selector that re-quotes what you would actually be charged, and a collapsible compare table whose cells derive from real plan fields rather than hand-maintained copy. The cycle toggle promotes a cycle rather than multiplying a monthly figure — each cycle is its own plan with its own rate.
- `[1.10]` Free-trial form · `LeadFormSection.tsx` · Two CMS-defined steps (fields, options, consent wording and every line of success and failure copy come from the section). Step one is name and number so an abandon at step two still leaves a contactable lead. Deep links carry intent: `/free-trial?branch=whitefield&class=hiit-45&intent=pt&plan=annual-all-access`. Per-field validation with `aria-invalid`/`aria-describedby`, off-screen honeypot, and a checkmark that draws itself on success.
- `[1.11]` Calculators · `CalculatorBlockSection.tsx` · BMI on the **WHO Asian-Indian cut-offs** (the international bands overstate a healthy range for this population and would mislead most readers of this page), and BMR via Mifflin-St Jeor with an activity multiplier and a ±15% goal adjustment. Both recompute on every keystroke, no network call.
- `[1.12]` Detail pages · `TrainerDetailPage.tsx`, `JournalPostPage.tsx` · Coach profiles with this-week's live classes, Person JSON-LD and a PT CTA; journal articles rendered from structured blocks (never HTML) with Article JSON-LD and a related rail. Both lazy-loaded so the home page's first paint pays for neither.
- `[1.13]` Imagery pass · `client/media.manifest.json`, `scripts/fetch-media.mjs` · **85 assets, 0 failures** — 8 trainer portraits, 14 facility shots, 10 class covers, 8 testimonial avatars, 5 journal covers, 5 transformation pairs and 14 per-page OG crops, on top of Phase 0's 16. Every one was sourced by searching Unsplash and reading the returned descriptions, then the portraits were opened and **visually reviewed**; five were rejected and re-sourced for carrying a competitor's wordmark (a rival yoga studio, "KINGS GYM", an NBA jersey) or an off-palette backdrop. The manifest gained `$group` heading rows and the fetcher skips them.

### Phase 1 verification — all gates pass

| Gate | Result |
|---|---|
| `dotnet build Gym.sln` | 0 errors, 0 warnings |
| `npm run build` | passes; vendor chunks split, both detail pages code-split |
| 15 new public endpoints | all 200 — formats, timetable (+filters), trainers, trainer/{slug}, plans, plans@branch, offer, testimonials, transformations, faqs, journal, journal/{slug}, sitemap.xml, robots.txt |
| Every section traceable to a `CmsSection` row | **14 pages, 63 sections, 22 types — every one has a renderer and every required field is present** (scripted check against the live API) |
| Media referenced by the CMS exists on disk | 31 referenced, 28 present; the 3 absent are the hero video pair and the app screenshot, both with working designed fallbacks |
| Per-branch pricing resolves | Whitefield quotes ₹2,900/₹7,700/₹13,700/₹18,300 against list ₹3,200/₹8,400/₹14,900/₹19,900; annual shows 48% saving vs monthly |
| Timetable has live data | 40 sessions over 7 days, 10 formats, 8 coaches, spots-left counts from real bookings |
| Lead capture end-to-end | create → `FRG-L00052` + 4 scheduled follow-ups; same number again folds into the same lead; honeypot answers 201 without storing; 3-digit phone → 400 |
| Performance basics | all below-fold media `loading="lazy"`, hero poster `eager`/`fetchPriority=high`, route-level code splitting, skeletons everywhere |

### Deviations from the docs — each with its reason

11. **Hero video still not sourced.** Trimming a Pexels clip to the specified 6–10s, muted, 2–4 MB WebM+MP4 pair needs ffmpeg, which is not installed here; shipping an untrimmed 4K clip with audio would breach 03 §4 and I could not view its content to check it was appropriate. The hero renders its poster, which is the designed fallback and looks finished. Listed in `media.manifest.json` with the exact command needed.
12. **Trainer portraits and transformation pairs are stock stand-ins.** 03 §4 wants one commissioned shoot and 02 §1.7 wants consented member photos. Both are flagged `REPLACE BEFORE LAUNCH` in the manifest — the transformation gallery in particular publishes named people with weights and timelines, so it must not ship with stock.
13. **Maps are OpenStreetMap, not Google.** No API key to leak, no consent banner, and the light tiles are inverted and hue-rotated back into the palette rather than punching a white rectangle through the page. A "Directions" link still opens Google Maps, which is what people actually want from a map on a gym page.
14. **`BranchLocator.nearestFirstPrompt` does not geolocate.** The seeded copy offers "Use my location to sort by distance"; the section rewrites it to state the branches are within 14 km of each other instead of prompting for a permission it would not act on. Advertising a control that does nothing is worse than not offering it.
15. **Online joining (1.11) stops at the checkout hand-off.** Plan cards route to `/free-trial?intent=join&plan={slug}`, which captures the lead with the plan attached. Razorpay order creation and the webhook are explicitly PROMPT 2's gate, so the payment leg lands there rather than being half-built twice.
16. **The app screenshot is drawn, not photographed.** `AppQrSection` renders the member-app home screen from live tokens inside the device frame — sharper than a PNG, and it cannot drift out of date with the palette. A real capture dropped at `/media/app/forge-app-home.png` takes over automatically.
17. **`ImageFeature` and `RichText` have renderers but no seeded content.** Both are registered and validated so the owner can add them to any page from the Phase 2 CMS without a deploy.

### What Phase 2 inherits

- The section registry and the Zod shapes in `sections/schemas.ts` are the exact contract the admin editors must produce — one structured form per shape and the public site cannot be broken by an edit.
- `registeredSectionTypes` is exported for the admin section picker.
- Leads arriving from the website already carry a scheduled follow-up sequence, so the pipeline board has real work in it on day one.
- `client/src/lib/public-api.ts` holds every public read model; the admin panel's write endpoints return the same shapes.

**Next step — PROMPT 2:** the admin panel — CMS editors against these shapes, then core ops (dashboard, members, billing with Razorpay, scheduling, attendance, leads board).

---

## PHASE 2 — Admin Panel: CMS + Core Ops ✅ COMPLETE (18 Aug 2026)

### 2A — CMS in full

- `[2.1]` Admin CMS API · `Gym.Api/Controllers/Admin/AdminCmsController.cs`, `Contracts/AdminCmsContracts.cs` · Pages (list with draft counts, detail, create, update, publish, unpublish, delete), sections (create, duplicate, delete, discard-draft, rename/rescope) and site settings (read every key including private ones, bulk update, create). Publishing a page promotes every pending section draft with it — leaving drafts behind is how stale copy ships. System pages can be edited but never renamed or deleted, because their slugs are wired into the router.
- `[2.2]` Media library · `Controllers/Admin/MediaController.cs` · Upload → WebP original plus a rendition at every width narrower than the source, plus a ~200-byte blurred data URI. Paged list with search/folder/kind filters, alt-text editing, folder taxonomy. **Alt text is required on an image upload** — a gallery of unlabelled photographs fails WCAG the moment the owner drops one into a section. Delete refuses while the URL is still referenced by a section, a coach portrait, a class cover, a journal post or a branch hero, and names what is using it.
- `[2.3]` Content library · `Controllers/Admin/AdminCollectionsController.cs` · CRUD for the four shared pools several sections read from: testimonials, transformations, FAQs and the journal. Transformation consent is a hard gate and is **timestamped on the transition**, because that gallery publishes a named person with their weight.
- `[2.4]` **Zod-driven section editors** · `client/src/features/admin/cms/{zod-fields.ts,SectionEditor.tsx,section-schemas.ts}` · The editor form for all 24 section types is *generated from the same Zod shapes the public renderer validates against* — imported, not copied. Adding a property to a section schema makes an input appear in the CMS with no further work, and a field cannot exist in the editor that the site cannot draw. Controls are inferred per type (string → text or textarea, media names → picker, enum → select, array-of-object → collapsible repeater with reorder, nested object → inline fieldset) and refined by a small label/hint table so the generated form still reads hand-written. **Nothing saves until the edited object passes that same shape**; the API stores whatever JSON it is handed, so this is the gate that keeps a bad edit off the site.
- `[2.5]` Page editor · `cms/CmsPageEditorPage.tsx` · Drag-reorder committed on drop **with keyboard arrows alongside it** — a list only reorderable by mouse-drag is a list a keyboard user cannot reorder at all. Per-section visibility, duplicate, delete, branch-scoping, a draft badge on every row, "add section" with a described picker for all 24 types, and a page SEO/state sheet with live length counters against the 60/160-character search limits.
- `[2.6]` Site settings & media screens · `cms/{SiteSettingsPage,MediaLibraryPage,ContentLibraryPage}.tsx` · Settings render by `valueType` (colour swatch + hex, media picker, toggle, textarea, url, number) grouped as the seeder grouped them, with unsaved-change badges and one save. Theme colours write straight to the CSS custom properties the whole site resolves through, so changing the accent repaints every button with no rebuild.

### 2B — Operations

- `[2.7]` Dashboard · `Controllers/Admin/DashboardController.cs`, `ops/DashboardPage.tsx` · Active members, MRR (every term normalised to a month so an annual and a monthly plan compare), today's check-ins, dues outstanding, expiring-in-7, new leads, collected-this-month, at-risk count — each against its comparison period. Plus 30/7/90-day revenue, footfall and joins series, per-branch comparison with class fill, plan mix, the churn radar and the renewals list. All live queries; there is no nightly rollup to go stale.
- `[2.8]` Members · `Controllers/Admin/AdminMembersController.cs`, `ops/{MembersPage,MemberDetailPage}.tsx` · Paged list with filters in the URL (so "Whitefield, expiring soon" is a bookmark), bulk tag/status/branch on the selection, CSV export with a BOM so Excel opens Indian names intact, and a **row-by-row CSV import** where one bad line is reported and skipped rather than failing the file. Detail carries the full profile, memberships, invoices, upcoming bookings, the QR card, and **one merged activity timeline** — joins, payments, visits, bookings and freezes in one reverse-chronological feed, which is the screen the desk actually reads before picking up the phone.
- `[2.9]` Billing · `Gym.Infrastructure/Services/{GstCalculator,InvoiceService,SubscriptionService}.cs`, `Controllers/Admin/AdminBillingController.cs` · Plan catalogue with the per-branch override table **inside** the plan editor, coupons with every cap enforced at the point of sale (and the reason surfaced when one does not apply), live quoting (branch override → admission fee once per member → coupon → proration credit → GST split), selling, freezing, resuming, cancelling and upgrading. GST is computed in one place: intra-state CGST+SGST halves, IGST when the customer GSTIN is out of state, statutory round-off carried as its own line. Invoice numbers are **gap-free within the Indian financial year** and account for rows added in the same unit of work. Invoice status is always *derived* from payments received and the due date, never set by hand.
- `[2.10]` Razorpay · `Services/RazorpayGateway.cs`, `Controllers/PaymentsController.cs` · Real order creation and constant-time HMAC verification for both the checkout handler and the `X-Razorpay-Signature` webhook. Creating an order writes a **Pending payment row keyed by the gateway order id**, which both the browser callback and the webhook settle — so a capture can arrive twice, out of order, or only over the webhook, and the invoice is credited exactly once.
- `[2.11]` Dunning & operations workers · `Gym.Infrastructure/BackgroundJobs/{DunningWorker,OperationsWorker}.cs` · The collections ladder at D-7, D-3, due date, then weekly, with state on the invoice so a restart never double-chases and never silently drops. The operations sweep keeps four weeks of sessions materialised, closes finished classes and marks no-shows, expires lapsed memberships, and raises the "no visit in 10 days" absentee alert (one per member per fortnight — chasing daily is how a win-back becomes spam). Both are runnable on demand from the UI.
- `[2.12]` Scheduling · `Services/SchedulingService.cs`, `Controllers/Admin/AdminSchedulingController.cs`, `ops/SchedulingPage.tsx` · Shared format library, per-branch rooms, and a weekly-grid recurring builder with **conflict detection as interval overlap on trainer and room, honouring both effective windows**. Editing a rule updates its future occurrences in place rather than deleting them, because members hold bookings against those exact rows; retiring a slot ends it today and removes only the unbooked occurrences. Rosters with attended/no-show marking, substitutions (checked for a clash), class cancellation that releases bookings, returns credits and notifies, and **waitlist auto-promotion on every cancel** with the queue renumbered so position 1 always means next.
- `[2.13]` Attendance & QR kiosk · `Controllers/Admin/AttendanceController.cs`, `ops/{AttendancePage,KioskPage}.tsx` · The kiosk answers in one round trip with everything the desk needs to act on — plan, expiry, dues, streak and today's booked classes — because a turnstile that only says "no" is one someone works around. Dues are a warning, not a locked door; expiry, freeze, wrong-branch and off-peak windows are refusals, and **a refusal is recorded with its reason** rather than discarded. Streaks advance on the first visit of a day. Plus the peak-hours heatmap on IST buckets, live occupancy, and the absentee list with one-click win-back. The kiosk runs the dark theme full-bleed and keeps focus in the scan field, because a hardware reader types into whatever has focus.
- `[2.14]` Leads pipeline · `Controllers/Admin/AdminLeadsController.cs`, `ops/LeadsPage.tsx` · Six-column board, drag *and* keyboard to move a card, and **every stage move schedules the follow-up that stage implies** — so the queue cannot drift from the board. Follow-up queue with completion and next-touch in one action, conversion analytics on the **median** first response (one lead answered three days late should not move the number the desk is judged on), and one-step conversion into a member with the plan sold and money collected — reusing an existing member if the number already belongs to one, rather than creating a duplicate person.

### Phase 2 verification — all gates pass

| Gate | Result |
|---|---|
| `dotnet build Gym.sln` | 0 errors, 0 warnings |
| `npm run build` | passes; charts split into their own chunk so the dashboard chunk is 12 kB, not 387 kB |
| `npx oxlint src` | no errors (only the pre-existing fast-refresh style warnings) |
| Swagger | **115 paths, 145 operations** (was 33 after Phase 1) |
| Scripted end-to-end suite | **53 checks, 53 pass** — see below |
| CMS proof | edited the hero headline and the brand tagline from the admin API and read the change back off the **public** endpoints; draft edits stayed invisible to the public while the admin saw them |
| Demo flow | lead `FRG-L00060` → board → trial stage → converted to `FRG-WHF-00063` → invoice `FRG/26-27/000208` → half by UPI → replayed key did **not** double-credit → settled by cash → booked a class → QR check-in admitted with plan/expiry/streak → visit on the attendance list |
| GST arithmetic | taxable 4190.47 + CGST 104.77 + SGST 104.76 + round-off = 4400.00 exactly; POS `29-Karnataka`, supplier GSTIN on the invoice |
| Waitlist | filled a class, cancelled one booking, **1 member promoted** off a 5-deep waitlist |
| Conflict detection | re-posting an existing slot reports 2 clashes naming the coach and the room; a 23:30 slot reports none |
| Freeze policy | 7-day freeze moved the end date 17 Sep → 24 Sep; resuming early handed the unused days back |
| Media pipeline | 2400×1350 PNG → WebP with 480/960/1440/1920 renditions + a 215-char blur URI, served at `/media/uploads/…`, deleted cleanly |
| Authorization | member → 403 on `/api/admin/members`, `/api/media` and CMS settings write; anonymous → 401 |

### Deviations from the docs — each with its reason

18. **Admin CMS writes are split across two routes.** The five write endpoints Phase 0 shipped stay at `/api/cms/*`; everything new is at `/api/admin/cms/*`. Moving the originals would have broken a working contract for no gain.
19. **Added a lenient `TimeOnly` JSON converter** (`Contracts/TimeOnlyJsonConverter.cs`). The API renders wall-clock times as `"06:00"` and an `<input type="time">` produces the same, but System.Text.Json only reads `"HH:mm:ss"` — so without it the timetable builder could not post back a slot it had just been shown. Found by the verification pass, not by inspection.
20. **`IFormFile` parameters carry no `[FromForm]`.** They bind from the multipart body regardless, and Swashbuckle throws generating the document when the attribute is present — which took `/swagger` down until it was removed. Also found by verification.
21. **Razorpay falls back to a sandbox simulator when no keys are configured — in Development only.** With keys it calls the real API. Without them, and only when `IsDevelopment()`, it issues a deterministic `order_SIM…` so the whole demo runs before the client's keys arrive; every such payment is stamped `razorpay-sandbox-simulator` on the row. In any other environment an unconfigured gateway *refuses to transact*, because a fake "paid" in production would corrupt the audit trail. **Same rule for the webhook:** with no `Razorpay:WebhookSecret` it accepts unsigned callbacks in Development and rejects them everywhere else — an unsigned webhook is an open endpoint for crediting invoices. Set `Razorpay:KeyId`, `KeySecret` and `WebhookSecret` in `appsettings.Development.json` to exercise the real sandbox.
22. **The QR kiosk is an admin-authenticated route** (`/admin/attendance/kiosk`), not a public one. The tablet signs in once; an anonymous check-in endpoint would let anyone on the network write attendance rows.
23. **Admin read models are TypeScript types, not Zod-parsed.** Zod runs where an arbitrary human edit can produce anything — CMS section content, validated against the exact shape the renderer uses. Admin list responses are server-shaped reads on an authenticated surface; re-parsing them would double the contract surface for no safety gained. `features/admin/lib/types.ts` mirrors `Contracts/Admin*.cs` one for one.
24. **`GET /api/admin/scheduling/trainers` added.** The public `/api/trainers` deliberately excludes coaches hidden from the website; the roster and substitution pickers must still be able to schedule them.
25. **Plans, coupons and class formats are retired rather than deleted when in use.** An invoice that references a deleted plan stops resolving, and history has to stay readable years later. The API says which it did and why.
26. **No migration was needed.** Phase 0's 47-entity schema covered Module 2 in full — including `Invoice.RemindersSent`/`LastReminderAtUtc` for dunning, `Payment.IdempotencyKey` for webhook retries and `Booking.WaitlistPosition` for promotion. `GymDb` is unchanged at 55 tables.
27. **The section editor is generated, not hand-written.** Twenty-four bespoke forms would drift from the schemas the day someone adds a field. Deriving them from the Zod shapes means the editor and the renderer cannot disagree — at the cost that a genuinely unusual field falls back to a raw-JSON textarea (nothing in the current 24 types does).
28. **Notifications are written but not delivered.** `Whatsapp:Provider` is `"none"`, so every dunning reminder, win-back, booking confirmation and cancellation writes its in-app row and queues the WhatsApp/SMS/email rows with an explicit "no provider connected" note. Nothing the system decided to say is invisible; connecting a provider is a single `INotificationDispatcher` change.

### What PROMPT 2 did **not** cover

02 §MODULE 2B also tables **Trainers & Staff payroll, POS & Inventory, Communications broadcasts, the Reports suite and Feedback/NPS**. PROMPT 2 scoped 2B to Dashboard, Members, Memberships & Billing, Classes & Scheduling, Attendance and Leads, and 02's own Build Phases put the rest in Phase 2/3. They are not built. The tables (`Products`, `BranchStocks`, `Orders`, `TrainerRatings`, `Notifications`, `FeedPosts`) are seeded and waiting.

**Next step — PROMPT 3:** the member portal — home with streak and occupancy, booking with optimistic UI and waitlist, My QR, membership self-serve via Razorpay, workout logging with PR detection, progress charts, referrals and notifications.
---

## PHASE 3 — Member Portal ✅ COMPLETE (18 Aug 2026)

- `[3.1]` Portal API surface · `Gym.Api/Contracts/PortalContracts.cs`, `Controllers/Portal/PortalControllerBase.cs` · **36 routes / 39 operations under `/api/portal`**, every one scoped to the signed-in member from the `member_id` claim and from nowhere else. No portal endpoint takes a member id in its route or body, so "own data only" (04 §4) is a property of the surface rather than a check each handler has to remember. `PortalSessions` holds the one booking-rule evaluator the timetable, the home screen and the booking endpoint all share, so a card can never say "Book" where the API would refuse.
- `[3.2]` Home · `Controllers/Portal/PortalHomeController.cs`, `PortalHome.tsx` · One round trip returns everything the first paint needs — identity, membership, streak with a 35-day calendar, live occupancy at the member's branch, today's classes, dues and the next invoice, unread count, announcements, the rating prompt, a pending PR celebration, new badges and the programme card. A member opens this on a phone outside the gym; eight parallel requests on a patchy connection is eight chances to show a half-drawn screen.
- `[3.3]` Booking · `PortalBookingController.cs`, `BookingPage.tsx`, `components/SessionCard.tsx` · Fortnight day rail, branch/format/time-of-day pills whose facets come back with the results, and **one-tap booking with optimistic UI** — the capacity ring fills and the button flips before the network answers, and the server's real counts replace the guess on the same round trip. A refusal restores the exact cache snapshot rather than refetching, so a failed tap never blanks the sheet. Waitlist join, position display, cancel with the late-cancel window named on the button, and auto-promotion (plus a SignalR `SessionCapacityChanged` / `WaitlistPromoted` push) on every cancel. Overlapping bookings are refused by name and time — a double-booked slot is a no-show waiting to happen.
- `[3.4]` My QR · `MyQrPage.tsx` · The digital membership card, drawn as an inline SVG QR rather than fetched as an image so it stays sharp and works with no signal — which is the state a member is in at a basement turnstile. It encodes the same `Member.QrToken` the desk kiosk matches on, so the two cannot drift apart, and the card says plainly when it would be refused *before* they walk in.
- `[3.5]` Membership self-serve · `PortalMembershipController.cs`, `MembershipPage.tsx`, `lib/razorpay.ts` · Current plan with a validity bar, credits, freeze allowance and features; plan catalogue priced at the member's own branch; live quoting; renew, upgrade and pay-an-open-invoice through Razorpay. **A renewal starts the day the current plan ends** so renewing early never costs the tail already paid for; an upgrade starts today and returns the unused days as a proration credit line. Nothing here re-implements pricing: every sale runs through the same `SubscriptionService.SellAsync` an admin sale runs, so an 11 pm renewal produces the same GST invoice, coupon accounting and audit trail as the same plan sold at the desk.
- `[3.6]` Freeze requests · `FreezeRequest` entity + migration, `PortalMembershipController`, `AdminBillingController`, admin `MemberDetailPage` · A freeze moves the end date and can carry a fee, so it is a *request* the desk answers rather than something a member does to themselves. The plan's allowance is checked at the point of asking, one pending request at a time, withdrawable until answered; the desk approves or declines with a reason from the member's own screen in the admin panel, and the approval runs the same `FreezeAsync` the desk's own button runs.
- `[3.7]` Workouts · `PortalTrainingController.cs`, `WorkoutsPage.tsx`, `components/RestTimer.tsx` · The assigned programme, day tabs, and a set logger built for a phone held in one hand between sets: weight and reps pre-fill from the last set, logging is one tap, and **every exercise arrives with what the member did last time on it** — the single number that keeps a paper diary competitive with most gym apps, and why the day payload is one request rather than one per lift. The rest timer counts against a wall-clock deadline rather than a `setInterval` tick, because a phone that sleeps between sets freezes the interval and a timer that quietly loses 90 seconds is worse than none.
- `[3.8]` PR engine · `Gym.Infrastructure/Services/TrainingService.cs`, `components/PrBanner.tsx` · Records are compared on **estimated 1RM (Epley), not raw weight** — a member who moves 60 kg for 8 has beaten their 65 kg single, and a weight-only comparison would tell them they had not. The verdict comes back on the same round trip the set was saved on, so the banner fires immediately; a record the member has to refresh to discover is not a celebration. The banner is a banner, not a modal, because nobody mid-session should have to dismiss a dialog before logging the next set. Badge rules are evaluated from the seeded `CriteriaJson`, so adding a badge is a data change; an unparseable rule never awards.
- `[3.9]` Progress · `PortalProgressController.cs`, `ProgressPage.tsx` · Composition trend, per-lift strength curves off the workout log (one point per session — the top set, because a chart of every warm-up is noise), weekly volume, the attendance calendar, body-scan entries and badges. **Scans the desk measured and scans the member typed in live on the same trend**, with the source carried on the row — a member who weighs themselves at home should not get a second, parallel history the InBody chart ignores. BMI is derived from height, never asked for.
- `[3.10]` Progress photos · `Gym.Infrastructure/Media/PrivateMediaStorage.cs` · Body photographs are stored **outside `wwwroot`**, where no static-file middleware is mapped, and streamed by an endpoint that checks the owner on every read. Filenames are random GUIDs, but that is belt-and-braces: "the URL is long" is not an access-control model for this kind of picture. Side-by-side compare picks any two dates in the same pose and shows the weight delta and the weeks between them.
- `[3.11]` Referrals · `PortalEngagementController.cs`, `ReferralsPage.tsx` · ₹500 both sides. An invitation is not just a rewards row: it **creates a real lead on the desk's pipeline board** with the referrer attached and the same five-minute follow-up every other lead gets, reusing an open lead on the same number rather than duplicating the person. A referral scheme nobody follows up on is a discount the gym pays for twice. Invitee numbers come back masked — the member typed it, but it is still someone else's.
- `[3.12]` Notifications & ratings · `PortalEngagementController.cs`, `NotificationsPage.tsx`, `components/RatingPrompt.tsx` · The centre shows **in-app rows only**; the WhatsApp and SMS copies of the same booking confirmation are delivery records, and listing them would triple every notification. The post-class prompt appears for classes actually attended in the last week (nobody remembers a class from last month well enough to answer honestly), is dismissible, and asks for a comment below three stars — the score the desk can act on. A rating of 2 or less writes an alert the same day rather than surfacing in a monthly report.
- `[3.13]` Portal shell · `MemberLayout.tsx`, `components/ui.tsx` · Dark surface on the same tokens as the public site. Bottom tab bar on a phone (five destinations plus More — six is where a thumb starts missing), horizontal rail on a desktop, unread badge on the bell, and one lazy chunk per screen so a member who only ever books a class never downloads the charting library. Sheets trap focus and close on Escape; every list has a real empty state.
- `[3.14]` Programme seeding · `Persistence/Seeding/ProgramSeed.cs` · Four coach-written templates (Foundation Strength 4-day, Lean Recomposition 3-day, Hybrid Athlete 5-day, Return to Training 3-day) with named lifts, real rep schemes, lettered supersets and a rest day that says what to do on it — plus a published copy assigned to each of the 72 members who already have training history. **76 programmes seeded.** An assignment is a deep copy: a member's plan must not change under them when the coach edits the template next month.

### Phase 3 verification — all gates pass

| Gate | Result |
|---|---|
| `dotnet build Gym.sln` | 0 errors, 0 warnings |
| `npm run build` | passes; portal split into 9 route chunks (largest 22 kB) — charts stay in the shared chunk the admin dashboard already pays for |
| `npx tsc -b --force` | 0 errors |
| `npx oxlint src` | 0 errors, 7 pre-existing fast-refresh warnings, none new |
| Swagger | **153 paths, 186 operations** (was 115/145) — 36 paths / 39 operations under `/api/portal` |
| Scripted end-to-end suite | **58 checks, 58 pass** on a freshly seeded database |
| Demo flow (the PROMPT 3 gate) | booked a class → QR check-in at the kiosk admitted and marked the booking **Attended** → logged a set → **PR detected on the same response** (72.0 kg e1RM) → banner pending on home, cleared on dismiss → rated the class 5★, second rating refused, prompt left the queue |
| Optimistic booking contract | server returned `13/16, 3 left` — exactly the counts the client had already guessed; double-book → 409; cancel returned the spot |
| Money leg | renewed through the sandbox simulator → invoice `FRG/26-27/000201` → order `order_SIM6D4BB45BEE5C36` → verify settled it → **CGST 200 + SGST 200 broken out**, replaying the callback did **not** double-credit |
| Freeze round trip | member requested 7 days → second request refused → desk saw it → approved → end date moved 15 Sep → 22 Sep |
| Progress photos | uploaded → owner read it (200, `image/webp`, `private` cache) → **anonymous read 401** → nothing written under `wwwroot` → deleted |
| Authorisation | anonymous → 401; an **admin** token → 403 on member-only routes; another member's invoice → 404 (not 403 — "that exists but is not yours" is itself a leak) |

### Deviations from the docs — each with its reason

29. **Three defects the gate surfaced were fixed rather than worked around.** (a) `SubscriptionService.FreezeAsync` marked a subscription `Frozen` the instant it was approved, so a freeze booked for next month locked the member out that afternoon; it now records the window and the operations sweep opens and closes it on its dates. (b) The kiosk skipped marking a booked class attended when the member already had an open visit, so anyone who came in for the floor at six and scanned again for the seven o'clock class lost the roster mark. (c) The portal keyed a dictionary on `ClassSessionId`, which throws the moment a member holds two rows against one session — a cancel-and-rebook does exactly that. All three are Phase-2 or Phase-3 code paths; none needed a schema change.
30. **One migration: `FreezeRequests`.** Every other Module 3 read and write fits Phase 0's schema — `WorkoutLog.IsPersonalRecord`/`PrCelebrated`, `Booking.RatingScore`, `MemberBadge.IsSeen`, `ProgressPhoto.IsPrivate` and `Referral` were all already there. `GymDb` is now 56 tables.
31. **Portal read models are TypeScript types, not Zod-parsed** — the same line Phase 2 drew (deviation 23). Zod earns its place where an arbitrary human edit can produce anything, which is CMS section content; these are server-shaped reads on an authenticated surface. `features/member/lib/types.ts` mirrors `Contracts/PortalContracts.cs` one for one.
32. **Progress photos are the only media stored outside `wwwroot`.** Owner-uploaded marketing media stays in the public library where a long URL is fine. A member's body photographs are not that, so they get a second storage root and an authenticated streaming endpoint. Added to `.gitignore`.
33. **Razorpay orders now open through one shared `PaymentOrderService`**, and `/api/payments/razorpay/{order,verify}` moved from `[Authorize(Roles=Admin)]` to `[Authorize]` plus an ownership check. A member paying their own invoice and the desk taking the same payment must settle through identical code, or the pending-row idempotency that makes webhook retries harmless would exist on only one of the two paths.
34. **The admin contract `FreezeRequest` was renamed `FreezeSubscriptionRequest`** to free the name for the entity. One call site; no client change.
35. **Best estimated 1RM is computed with its own grouped query**, not read off the 600-row history window the day payload loads. A best-ever figure taken from a capped page can understate a record, and understating a member's own best is the one number this screen must never get wrong.
36. **A member can edit their own injury and medical notes.** 02 §Members puts them on the admin form. The person with the shoulder knows about it before the coach does, and a field only the desk can fill in is a field that stays wrong.
37. **The rest timer counts to a wall-clock deadline** rather than decrementing on a tick, and re-reads the clock on `visibilitychange`. Phones sleep between sets; a throttled interval silently loses the rest period.

### What PROMPT 3 did **not** cover

02 §MODULE 3 also tables **Diet** (assigned meal plan with macros and daily adherence), **Gamification** beyond streaks and badges (monthly challenges, branch leaderboard), the **Community feed**, the **PT marketplace**, the **VOD library** and **help tickets**. PROMPT 3 scoped the portal to home, booking, My QR, membership self-serve, workouts, progress, referrals, notifications and the post-class rating prompt, and 02's own Build Phases put the rest in Phase 2/3. They are not built. The tables (`DietPlans`, `Meals`, `Challenges`, `FeedPosts`) are seeded or ready, and the PR engine already writes a `FeedPost` for members who have opted the leaderboard in — so the community feed has real content waiting the day it gets a renderer.

### What Phase 4 inherits

- `PortalSessions.Map` is the single place a session's bookability is decided; the SignalR `SessionCapacityChanged` and `WaitlistPromoted` pushes already fire from booking and cancellation, so Module 4.1's live meter needs a client subscription rather than a server change.
- `TrainingService.LogSetAsync` already writes the `FeedPost` and the notification a PR shout-out needs, gated on `ConsentLeaderboard`.
- `WorkoutProgram` carries `Author`, `GenerationContextJson`, `ApprovedBy` and `ApprovedAtUtc`, and the portal renders `Author` as "AI draft, coach approved" — the generator (Module 4.2) writes a Draft and an admin publishes it; nothing in the viewer changes.
- `BodyScan` history and the strength series are the data the shareable body-scan PDF (Module 4.4) reports on.

**Next step — PROMPT 4:** the differentiators and the polish pass — live occupancy over SignalR end to end, churn-risk radar with one-click win-back, the AI workout/diet generator behind an interface, the body-scan PDF, the PR/streak engine posting to a community feed, corporate codes, the seasonal offer engine, then 03 §6–9 polish and `DEMO.md`.

---

## PHASE 4 — Differentiators & Polish ✅ COMPLETE (18 Aug 2026)

### Module 4 — the features "no gym around has"

- `[4.1]` Live occupancy end to end · `Gym.Infrastructure/Services/OccupancyService.cs`, `Gym.Api/Hubs/SignalROccupancyNotifier.cs`, `client/src/lib/realtime.ts` · One service computes a branch's head-count and every surface reads it, so the REST first paint and the socket push can never disagree. `IRealtimeNotifier` lives in Core with a no-op default; the API replaces it with the SignalR implementation, which lets the background sweep and the design-time tooling stand alone without the hub. Check-in, check-out and lock-up all push. The client holds **one connection per tab, reference-counted per group** — a page with three branch meters opens one socket, and the last component to unmount releases it. Group membership is re-joined on reconnect, because SignalR does not restore it. Polling stays underneath at two minutes as the floor: a browser that cannot hold a socket still gets a moving number, and a stale frame arriving after a fresh poll loses on its timestamp.
- `[4.1b]` Typical busy hours · `BranchesController.TypicalHours`, `client/src/features/public/components/TypicalHours.tsx` · Eight weeks of check-ins averaged per weekday hour on the **IST wall clock**, with the current hour marked and today's column selected on arrival. The gauge answers "should I come now"; this answers "when should I come", which is the question someone actually has when they open the page from their desk. The bar chart is `aria-hidden` with the finding written out in a sentence for a screen reader.
- `[4.3]` Churn radar · `Gym.Infrastructure/Services/ChurnService.cs`, `Controllers/Admin/AdminChurnController.cs`, `ops/ChurnRadarPage.tsx` · Rules, not a model, so the desk can read the score back to the member. Six signals: **visit-frequency drop across two windows** (someone who trained four times a week and now trains once is leaving, even though "once a week" alone looks fine), unpaid and overdue invoices, failed payments, class ratings of 2 or below, nothing booked ahead, and a term about to expire. Six grouped queries for 200 members rather than 1,200. Every row carries **why** it was flagged and the header carries the revenue attached to the flagged rows.
- `[4.3b]` One-click win-back · Mints a member-specific coupon **through the existing coupon engine**, so every cap and expiry the point of sale already enforces still applies, messages the member on each enabled channel, and puts a call-back on the desk's own board. A second win-back inside a fortnight is refused with the date of the last one; the desk can override, deliberately. The absentee list's win-back button now runs the same code path, so there is one implementation of "send a win-back" rather than two that drift.
- `[4.2]` Plan generator behind an interface · `Gym.Core/Interfaces/PlanGeneration.cs`, `Services/PlanGeneration/**`, `Controllers/Admin/AdminPlanStudioController.cs`, `ops/PlanStudioPage.tsx` · `IPlanGenerator` with two implementations reading an identical context object. **Claude** (`claude-opus-5`, official Anthropic SDK) uses structured outputs so a draft either arrives in the exact shape the editor renders or the call fails loudly; returned exercise ids are validated against the branch's own library. **Rule-based** is not a degraded mode — it is what the gym runs on with no key, so it produces a split a trainer would sign off: prescription by goal and experience, working weights from the member's own recorded bests rounded to the 2.5 kg plates the floor actually has, keyword-audited injury contraindications, and rest days that say what to do on them. Diet uses Mifflin-St Jeor — the same formula as the public BMR calculator, so a member cannot get two different numbers from one product.
- `[4.2b]` Approval is the gate · Every draft is written `Draft` and no member can see it until an admin publishes. Publishing stamps the approver, archives what they were following and tells them. A published plan refuses further edits — a member is following it. The engine, the inputs and any fallback reason are stored on the draft, so a plan can be answered for a year later.
- `[4.4]` Body-scan PDF · `Gym.Infrastructure/Reporting/BodyScanReport.cs`, `Services/ProgressReportService.cs` · QuestPDF on the brand's own palette, because a member forwards this to their family. Composition trend, strength as first-versus-latest **estimated 1RM per lift** (a story about change, so "first" is by date rather than by magnitude), visits, sessions and records. Every figure is read before the document is composed — QuestPDF renders synchronously and a lazy navigation inside the layout would print nothing.
- `[4.5]` PR/streak engine and community feed · `TrainingService.RecordStreakMilestoneAsync`, `Controllers/Portal/PortalCommunityController.cs`, `Controllers/Admin/AdminFeedController.cs`, `member/CommunityPage.tsx`, `ops/FeedPage.tsx` · Records already posted themselves; streaks now do too, but **only at milestones** — a feed that announces every consecutive day is one nobody reads by Friday, and it buries the records it exists for. Consent is checked when the post is written **and again when the feed is read**, so a member who withdraws it disappears today rather than from tomorrow's posts. A member's record can be hidden but never deleted. WhatsApp share card with the server composing the text so the numbers cannot drift from the log.
- `[4.6]` Corporate memberships · `Gym.Core/Entities/Corporate.cs`, `Services/CorporateService.cs`, `Controllers/Admin/AdminCorporateController.cs`, `ops/CorporatePage.tsx`, portal enrolment on the membership screen · Employees self-enrol with a code instead of the desk keying in forty people. Enrolment is its own row rather than a flag on the member, so leaving the company releases the seat and keeps the history. The HR export leads with **how many enrolled employees actually turn up** — a seat nobody uses is a seat they cut next year — and CSV carries a BOM so Excel opens Indian names intact.
- `[4.7]` Seasonal offer engine · `Services/OfferService.cs`, `Controllers/Admin/AdminOffersController.cs`, `ops/OffersPage.tsx` · **A campaign is a coupon** — the same row the point of sale validates — so what the website advertises and what the desk can apply are one object. What this adds is the campaign's life: scheduled start, automatic retirement when it expires or sells out, one banner at a time, and what each one actually earned. The off-peak tier is on the same screen with its subscriber count and how many check-ins were refused outside the window.

### The polish pass (03 §6–9)

- `[4.8]` Icons and manifest · `client/scripts/make-icons.mjs` · `apple-touch-icon.png` was referenced in `index.html` and did not exist — a 404 on every iOS visit. The barbell mark is now rasterised from the same geometry as `favicon.svg` by a hand-rolled PNG encoder (distance fields plus `zlib`), producing the touch icon, both manifest sizes and a **maskable** variant inset for Android's crop. No native image dependency to install on a machine that clones this. Plus a web manifest and the OG dimension/alt tags.
- `[4.9]` Contrast audit · `client/scripts/contrast-audit.mjs` (`npm run a11y`) · Reads the palette out of the stylesheets rather than restating it, so a token edit either still passes or breaks the audit, and exits non-zero on failure. It found four genuine problems: **form-control borders below the 3:1 that WCAG 1.4.11 requires** on both surfaces, and gold and signal red falling under AA as text on the admin's paper surface. Fixed with a `--field-line` token for control boundaries and darker instances of gold and red in `.theme-light` — same hues, lower values. **26 of 26 pairings now meet AA.**
- `[4.10]` Reduced motion and hooks · The mobile navigation sheet slid the full width of the screen under `prefers-reduced-motion` — the global CSS rule cannot reach a JS-driven Framer transform, so it needed `useReducedMotion` explicitly. Also fixed a `useState` called after an early return in `ProgressPage` (a real rules-of-hooks violation the linter caught).
- `[4.11]` Undefined design tokens · `text-fog`, `--warn` and `--danger` were used in 21 places across the new screens and defined nowhere, so they silently painted nothing. Mapped onto the palette that exists: `text-smoke`, `--accent`, `--accent-hot`.
- `[4.12]` Responsive imagery · `client/scripts/optimise-media.mjs`, `src/components/ui/Photo.tsx` · Lighthouse's largest finding by far: the site served 4K masters to every device — 3.8 MB of over-sized images on the home page alone. `sharp` now writes a WebP at each width the layouts actually request (210 renditions across 70 sources) and records them in a manifest the `<Photo>` component reads to build a srcset. **The home hero went from a 2,286 kB JPEG to a 74 kB WebP — 97% less, for the same picture.** Two traps found on the way: `src` alongside `srcset` makes Chrome fetch the fallback *as well as* the chosen candidate, so `src` is omitted when renditions exist; and the hero's `<video poster>` was quietly pulling a second full-size copy of a frame the `<img>` behind it already displayed. Originals stay on disk as the master and the fallback for anything the pipeline has not processed.
- `[4.13]` Heading order and small text · Four sections rendered their card titles at a fixed `h3`/`h4` while their own `SectionHeader` only emits an `h2` **when the CMS gives that section a headline** — so on a page where the owner left the headline blank, the document skipped a level. Each now derives its heading level from whether that heading actually rendered. Separately, `text-smoke/70` and `/60` (4.27:1 and 3.39:1) were dimming already-secondary text below AA in 15 places; size and weight mark those as secondary well enough without the colour doing it too.

### Post-review fixes (19 Aug 2026)

Raised after a walkthrough of the admin panel: unreadable text, the leads cards, and pages dragging the sidebar as they grew.

- `[4.14]` **The light theme never reached most of the admin panel.** `@theme` declares `--color-carbon: var(--carbon)` on `:root`, so that `var()` resolves against the *dark* value there and inherits downwards as an already-computed literal — re-declaring `--carbon` inside `.theme-light` could never change it. Every generated utility (`bg-carbon`, `text-bone`, `text-smoke`, `bg-ink`…) therefore kept painting the dark palette while rules written as `var(--token)` correctly switched, which is why the sidebar rendered near-black under near-black text: **1.05:1, invisible**. `.theme-light` now overrides Tailwind's own `--color-*` names alongside the base tokens. A rendered audit across twelve admin screens went from **807 contrast findings to 0**.
- `[4.15]` Admin status colours · The confirmation mint (`#3ecf8e`) is a dark-surface value and measured **1.9:1 on white** — every "Active", "Paid", "Attended" and positive delta in the panel. The light surface now runs its own darker green, and secondary text was darkened so it also clears AA on the thumbnail and avatar fill it lands on. Six admin pairings added to `npm run a11y`, which now covers 33.
- `[4.16]` Admin shell scrolling · The shell was `min-h-dvh`, so a long table grew the page and scrolled the sidebar and toolbar off the top. The shell is now exactly one viewport with the content column owning the scroll; sidebar and toolbar stay put. Verified by driving the real page: content scrolled 1,200px with both chrome elements unmoved and `window.scrollY` still 0.
- `[4.17]` Leads board · Columns were unbounded, so the board grew the page instead of scrolling inside their own lists and the stage names scrolled away — they are now bounded on large screens. The per-card move/call/WhatsApp controls were `opacity-0` until hover, which on the desk tablet meant unreachable; they are visible by default and only fade-until-hover where a real pointer exists. Long plan names ("Personal Training — 12 Sessions") overflowed the card and now truncate.
- `[4.18]` **Phone numbers were doubled and the links were broken.** Numbers arrive in E.164 (`+919876543210`) and nine call sites prefixed another `+91` — displaying `+91 +9198…` and building `tel:+91+9198…` and `wa.me/91+9198…`, so **every call and WhatsApp button on the leads board, the dashboard, attendance and invoices dialled nothing**. One pair of helpers (`formatPhone`, `telLink`) now owns the display and the link, and renders `+91 98765 43210`.
- `[4.19]` `client/scripts/audit-admin.mjs` · The audit that found all of this: it signs in, walks every visible text node on twelve screens, reads the *computed* colour against the colour actually painted behind it, checks the shell scrolls its content rather than the page, and screenshots each screen. Token-level reasoning could not have caught `[4.14]` — the tokens were right; the cascade was not.

### Phase 4 verification — all gates pass

| Gate | Result |
|---|---|
| `dotnet build Gym.sln` | 0 errors, 0 warnings |
| `npm run build` | passes; SignalR isolated in its own 57 kB chunk, loaded only where the meter is |
| `npx tsc -b --force` | 0 errors |
| `npx oxlint src` | 0 errors, only the pre-existing fast-refresh warnings |
| Swagger | **190 paths, 228 operations** (was 153/186) |
| `node server/verify-module4.mjs` | **50 checks, 50 pass** |
| `node server/verify-occupancy-push.mjs` | kiosk check-in → **OccupancyChanged frame over WebSockets in under a second**; push and REST agree (18 → 19 on the floor) |
| `npm run a11y` | 33 of 33 pairings meet WCAG AA on both surfaces |
| `node scripts/audit-admin.mjs` | 12 admin screens · **0 contrast findings** on what is actually rendered · content scrolls, page does not |
| Lighthouse — accessibility | **100 on all 11 public pages** (was 95–98; found real heading-order and contrast defects) |
| Lighthouse — SEO | **100 on all 11 public pages** |
| Lighthouse — best practices | 96 (the one deduction is the 401 the silent-refresh probe logs when signed out) |
| Lighthouse — performance | 57–72 · LCP 4.3–5.8 s · CLS 0.091 worst · TBT ~240 ms. **LCP does not meet the sub-2.5 s NFR** — see deviation 49 |
| Churn radar | 187 members scored; 43 red, 9 amber; ₹68,900 of plan value at risk; every row carries its reasons |
| Win-back | sent with coupon `BACKF00005` across 4 channel rows → second attempt **409 with the date of the last one** → forced override accepted |
| Plan generator (no key) | rule-based draft: 5 days, 20 exercises, rest days instructed, `Draft` until published |
| Plan generator (bad key) | reached Anthropic, got a real `authentication_error`, **fell back to rules and recorded why on the draft** — the draft still arrived complete |
| Diet generator | 2,150 kcal / 150 g protein; meals sum to within 10% of target; vegetarian plan names no meat, fish or eggs |
| Publish | stamps approver, archives the previous plan, notifies the member; editing afterwards → 409 |
| Corporate | 2 accounts, 33 seats; HR report separates 10 training from 1 idle; CSV exports with a UTF-8 BOM |
| Offers | banner set → the public `/api/plans/offer` returns the same code; expired campaign refused the banner |
| Body-scan PDF | 51.4 kB `application/pdf`, `no-store`, **401 to an anonymous reader** |
| Authorisation | member → 403 on the radar; admin token → 403 on member-only routes; corporate → 401 anonymous; typical hours stay public |

### Deviations from the docs — each with its reason

38. **The seasonal offer engine has no table of its own.** 02 §4.7 asks for "coupon campaigns with expiry and usage caps", which `Coupons` already is — it carries validity dates, usage and per-member caps, branch and plan scope, and the banner headline. Adding a parallel `SeasonalOffer` entity would have created a second discount system for the point of sale to reconcile with. The engine is the lifecycle around the existing row: scheduled activation, automatic retirement, one banner at a time, and per-campaign performance.
39. **`IPlanGenerator` is registered against the rule-based implementation, not Claude.** The interface resolves to the path that always works; `PlanGenerationService` reaches for Claude first when a key is configured and falls back without the caller knowing. Registering the model as the default would make an outage look like a 500 to the desk instead of a plan with a note on it.
40. **The Claude call uses structured outputs rather than prompt-and-parse.** `output_config.format` constrains the response to the JSON Schema, so a malformed draft is impossible by construction rather than caught by a parser. Returned exercise ids are still validated against the library — the schema cannot know which lifts this branch owns.
41. **Model pinned to `claude-opus-5` in configuration, not floating.** A plan the owner approved should not change shape under them because a default moved. The Phase 0 placeholder said `claude-sonnet-5`; it now names the current Opus and the section carries `MaxTokens`, `TimeoutSeconds` and `Enabled` alongside.
42. **Corporate discounts are recorded on the account, not applied automatically at the till.** Enrolment stores the benefit and the member's screen states it; the sale still runs through the same quoting path the desk uses. Wiring a second automatic discount source into pricing alongside coupons is how two discounts silently stack.
43. **Two new tables and four columns.** `CorporateAccounts` and `CorporateEnrolments`, plus `Member.ChurnReasons`, `ChurnScoredAtUtc`, `LastWinBackAtUtc` and `CorporateAccountId`. Everything else in Module 4 fits the Phase 0 schema — `FeedPost`, `WorkoutProgram.Author`/`ApprovedBy`, `Coupon.ShowAsWebsiteBanner` and `Plan.AccessWindowStart`/`End` were all already there. `GymDb` is now 58 tables.
44. **QuestPDF for the report, under the Community licence.** Free below the vendor's revenue threshold, commercial above it — the same shape of question as ImageSharp (deviation 5). **Both are worth confirming with the client before launch.**
45. **The community feed has no composer.** Members cannot post; records and milestones post themselves and the gym posts announcements. A feed people can write into is a moderation job the owner did not ask for, and 02 §4.5 asks for PR celebrations posted to a feed, not a social network.
46. **The admin's light surface runs darker gold and red.** Both brand values are chosen against near-black and fall under AA as text on paper. `.theme-light` overrides them to `#8a6d1c` and `#c22d19` — same hues, lower values — which is also the only way the gold button's label passes, since `--ink` remaps to paper on that surface.
47. **Win-back messages are queued, not delivered.** Unchanged from deviation 28: `Whatsapp:Provider` is `"none"`, so the in-app row is written and the WhatsApp and email rows are queued with an explicit note. The sequence, the cool-off and the desk task are all real; only the transport is pending.
48. **The AI path is verified against a rejected key, not a live one.** With no key available, the suite proves the machinery that matters: the request genuinely reaches `api.anthropic.com`, the failure is caught, the rule-based plan stands in, and the reason is recorded on the draft. **Generation quality against a live key is untested and should be reviewed once a key is added.**

49. **LCP is 4.3–5.8 s and the NFR asks for under 2.5 s.** Image weight is no longer the cause — the hero is 74 kB and every opportunity Lighthouse lists for images is now clear. What remains is architectural: this is a client-rendered SPA, so nothing paints until the bundle downloads, parses, and the CMS request returns, which under Lighthouse's throttled mobile profile costs ~3.7 s of FCP before the image is even requested. Closing the gap means server-rendering or pre-rendering the public routes — a different application shape from the Vite SPA 04 §2 specifies, and beyond a polish pass. **Flagged for the client as the one NFR not met.** Everything cheaper has been done: responsive WebP, code splitting, lazy media, eager high-priority hero.
50. **Non-blocking font loading was tried and reverted.** Deferring the two font stylesheets (`media="print"` then promote) is the standard trick and it measured no reliable first-paint win here while roughly doubling layout shift as the display face swapped under the headline — CLS 0.091 → 0.175. Both sheets already carry `display=swap`, so text is never invisible. Kept render-blocking on purpose, which is the opposite of the usual advice and the right call for this page.

### What PROMPT 4 did **not** cover

02 §MODULE 4 also lists **form-check video uploads** (4.8), which the specification itself places in Phase 3 behind a premium PT tier. It is not built. The rest of Module 4 is complete.

The Phase 2 and Phase 3 gaps recorded earlier stand unchanged: payroll, POS and inventory, the reports suite, broadcasts and NPS; diet adherence, challenges and leaderboards, the PT marketplace, VOD and help tickets. All are Phase 2/3 items in 02's own build order, and their tables are seeded and waiting.

**Next step:** `DEMO.md` walks the client through every feature with the two logins. Before launch: Razorpay keys, a WhatsApp provider, an Anthropic key if Claude is to write the drafts, commissioned photography for the trainer and transformation galleries, the hero video, and confirmation on the ImageSharp and QuestPDF licences.

---

## How to run

```powershell
# API — http://localhost:5080 (Swagger at /swagger). Migrates and seeds on startup, idempotently.
cd server\Gym.Api; dotnet run --urls "http://localhost:5080"

# Client — http://localhost:5173 (proxies /api, /media/uploads, /hubs to the API)
cd client; npm run media   # once, to download brand imagery
cd client; npm run dev
```

To rebuild the demo database from scratch: drop `GymDb`, then `dotnet ef database update -p Gym.Infrastructure -s Gym.Api` and start the API. A design-time `GymDbContextFactory` keeps `dotnet ef` from booting the app's startup migrate-and-seed block as a side effect.

### Where things are

| Surface | URL | Login |
|---|---|---|
| Public site | `http://localhost:5173/` | none |
| Member portal | `/portal` · `/book` `/qr` `/workouts` `/progress` `/community` `/membership` `/referrals` `/notifications` `/profile` | any seeded member email · `Member@12345` |
| Admin panel | `/admin` · `/churn` `/plan-studio` `/feed` `/offers` `/corporate` | `admin@gym.local` · `Forge@Admin2026!` |
| Desk kiosk | `/admin/attendance/kiosk` | the admin session |
| Swagger | `http://localhost:5080/swagger` | paste an access token |

Two background workers start with the API: the dunning sweep every six hours and the operations sweep every two. Both can be run on demand from **Collections → Run the ladder now** and `POST /api/admin/attendance/run-sweep`. The operations sweep also opens and closes membership freeze windows on their dates, so a freeze approved for next month does not lock the member out today.

### Verification scripts

```powershell
node server/verify-module4.mjs           # 50 checks across every Module 4 feature
node server/verify-occupancy-push.mjs    # a kiosk check-in must reach a subscribed socket
cd client; npm run a11y                  # WCAG AA contrast across both surfaces
cd client; npm run icons                 # regenerate the PNG icons from the brand mark
cd client; npm run media:optimise        # rebuild the responsive WebP renditions
```

`DEMO.md` is the client-facing walkthrough: every feature in the order it makes sense to show it, with the two logins.
